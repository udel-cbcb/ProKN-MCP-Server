[pytest]
# Offline tests run by default. Live tests need a running Neo4j (for the
# smoke suite, a running MCP server); use`-m live`, skip with `-m "not live"`.
markers =
    live: requires network access to a running Neo4j / ProKN MCP server
testpaths = tests
